<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/baseScreenDrawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activities.BaseActivity"
    tools:openDrawer="start">

    <FrameLayout
        android:id="@+id/baseScreenFrameLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent"/>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/baseScreenCoordinatorLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.core.widget.NestedScrollView
            android:id="@+id/baseScreenNestedScrollView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/topAppBarLayout"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:gravity="center_vertical"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_scrollFlags="scroll|enterAlways">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/topAppBar"
                style="@style/BaseActivityToolbar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                app:layout_scrollFlags="scroll|enterAlways"
                app:buttonGravity="center_vertical"
                android:paddingTop="@dimen/mediumPadding"
                android:paddingStart="@dimen/mediumPadding"
                android:paddingEnd="@dimen/mediumPadding"
                android:paddingBottom="@dimen/mediumPadding"
                android:elevation="0dp"
                app:titleTextColor="@android:color/white"
                app:navigationIcon="@drawable/menu_icon" >

                <TextView
                    android:id="@+id/baseScreenToolbarTextView"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/baseScreenText"
                    android:layout_marginStart="@dimen/mediumMargin"
                    android:layout_marginEnd="@dimen/mediumMargin"
                    android:textSize="@dimen/extraLargeText"
                    android:gravity="center_vertical"/>

            </com.google.android.material.appbar.MaterialToolbar>

        </com.google.android.material.appbar.AppBarLayout>

        <com.google.android.material.bottomappbar.BottomAppBar
            android:id="@+id/bottomAppBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:hideOnScroll="true"
            android:backgroundTint="@color/baseScreenBackground"
            android:layout_gravity="bottom"
            app:fabCradleRoundedCornerRadius="@dimen/mediumMargin"
            app:fabCradleVerticalOffset="@dimen/smallMargin"
            app:contentInsetStart="0dp">

            <com.google.android.material.bottomnavigation.BottomNavigationView
                android:id="@+id/bottomNavigationView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bottom_background_transparent"
                app:labelVisibilityMode="labeled"
                app:itemIconTint="@color/bottomNavigationItem"
                app:itemTextColor="@color/bottomNavigationItem"
                app:menu="@menu/bottom_bar_menu" />

        </com.google.android.material.bottomappbar.BottomAppBar>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabMenu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/baseScreenBackground"
            app:layout_anchor="@id/bottomAppBar"
            app:srcCompat="@drawable/fab_menu" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/baseScreenNavigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:scrollIndicators="end"
        android:theme="@style/NavigationTheme"
        app:headerLayout="@layout/navigation_header"
        app:itemIconTint="@color/menuIcon"
        app:itemTextColor="@color/menuText"
        app:menu="@menu/navigation_menu" />

</androidx.drawerlayout.widget.DrawerLayout>